# API Configuration
API_CORS=http://localhost:4200
API_NAME=Apoio-Solidario
API_PORT=8080
CONTEXT_PATH=/api/v1

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=apio_db
DB_USER=docker
DB_PASSWORD=docker
DB_TYPE=postgresql
DB_DRIVER=<drive>

# Application Profile
PROFILE=dev

# Mail Configuration
MAIL_HOST=
MAIL_PORT=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_AUTH=
MAIL_START_TLS=

# Swagger Configuration
SWAGGER_LINK=/swagger-ui/index.html
